---
import MarkdownToHtmlConverter from '../components/MarkdownToHtmlConverter.astro';
import ToolLayout from '../layouts/ToolLayout.astro';
---

<ToolLayout
  title="Markdown to HTML Converter — Free browser tool | Bizzon"
  description="Free browser-based Markdown to HTML converter by Bizzon. Paste Markdown or open a local .md file, preview safe HTML, then copy or download the result locally."
  canonicalPath="/markdown-to-html"
  heading="Markdown to HTML Converter"
  intro="Convert Markdown into clean HTML with a safe browser-only preview, copy-ready output, and downloadable .html export."
>
  <MarkdownToHtmlConverter />
  <section class="mt-12 grid gap-6 md:grid-cols-3">
    <div class="rounded-3xl border border-slate-200 bg-white p-6"><h2 class="text-xl font-bold text-slate-950">Preview before copying</h2><p class="mt-3 text-slate-600">Use the side-by-side output and preview to check headings, lists, links, quotes, and code before you paste or download HTML.</p></div>
    <div class="rounded-3xl border border-slate-200 bg-white p-6"><h2 class="text-xl font-bold text-slate-950">Safer by default</h2><p class="mt-3 text-slate-600">Raw HTML from your Markdown is escaped, so script tags and unsafe snippets show as text instead of running in the preview.</p></div>
    <div class="rounded-3xl border border-slate-200 bg-white p-6"><h2 class="text-xl font-bold text-slate-950">Runs in your browser</h2><p class="mt-3 text-slate-600">Bizzon does not upload or store pasted drafts or local files. The conversion happens locally on your device.</p></div>
  </section>
</ToolLayout>
