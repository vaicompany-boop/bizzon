export const toolLimitations: Record<string, string> = {
  "word-counter": "Counts use UTF-16 code units for characters, so some emoji count as two or more. Words are groups of letters or numbers, with internal apostrophes and hyphens retained. Reading time assumes 200 words per minute and rounds up; sentence counts use punctuation heuristics and paragraphs count nonempty lines. These are estimates, not language-aware analysis.",
  "character-counter": "Characters are UTF-16 code units, not visible symbols: an emoji can count as two or more. Without spaces removes whitespace including tabs and line breaks. Words use letter/number groups; paragraph counts use nonempty lines. Always confirm a strict limit in the destination form.",
  "filename-cleaner": "This tool produces names, not renamed files. Only the final dot is treated as the extension separator. Cleanup retains ASCII letters and numbers; other scripts may be removed. Duplicate suffixes help with repeated names but do not check your folder or guarantee collision-free output. Review the entire batch before renaming.",
  "qr-code-generator": "This creates a static QR code containing the trimmed input, without checking that a URL is reachable. Downloaded codes cannot be edited here. The image has a two-module margin; add blank space around it in your layout and scan the final print. Higher error correction can make a code denser; it does not guarantee readability.",
  "text-cleaner": "Cleanup changes plain text, not document formatting. Fix extra spaces collapses tabs and ordinary spaces and trims lines. Remove empty lines deletes blank separators. Normalize smart quotes also changes en/em dashes to hyphens. It does not repair OCR mistakes, join PDF line wraps, or remove every invisible Unicode character."
};
